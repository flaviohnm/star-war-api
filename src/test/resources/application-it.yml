### DATABASE DEFINITION ###
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/db_star_wars_test?useTimezone=true&serverTimezone=${TZ:America/Sao_Paulo}
    username: ${MYSQL_USER:dbuser}
    password: ${MYSQL_PASSWORD:password}
