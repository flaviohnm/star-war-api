### DATABASE DEFINITION ###
spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver